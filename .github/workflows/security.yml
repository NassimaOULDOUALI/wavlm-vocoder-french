name: Security Scan

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]
  schedule:
    - cron: '0 0 * * 0'
  workflow_dispatch:

# AJOUT IMPORTANT: Donner les permissions nécessaires
permissions:
  contents: read
  security-events: write  # Permet d'uploader les résultats SARIF
  actions: read

jobs:
  security:
    name: 🔒 Security Scan
    runs-on: ubuntu-latest
    timeout-minutes: 10
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
      
      - name: 🔧 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'
      
      - name: ⬆️ Upgrade pip
        run: python -m pip install --upgrade pip
      
      - name: 📦 Install bandit
        run: |
          pip install bandit==1.7.10
          pip install bandit-sarif-formatter
      
      - name: 🔍 Run Bandit security scan
        id: bandit
        continue-on-error: true
        run: |
          # Scan et rapport texte
          bandit -r src/ -f txt -o bandit_report.txt || true
          
          # Scan et rapport SARIF (pour GitHub Security)
          bandit -r src/ -f sarif -o bandit_results.sarif || true
          
          # Compter les issues
          echo "## 📊 Security Scan Results" > bandit_summary.md
          echo "" >> bandit_summary.md
          echo "\`\`\`" >> bandit_summary.md
          bandit -r src/ -f txt || echo "No issues found" >> bandit_summary.md
          echo "\`\`\`" >> bandit_summary.md
      
      - name: 📊 Upload SARIF to GitHub Security tab
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: bandit_results.sarif
          category: bandit
        if: always() && hashFiles('bandit_results.sarif') != ''
        continue-on-error: true  # Évite de faire échouer le workflow si problème de permission
      
      - name: 📤 Upload bandit report
        uses: actions/upload-artifact@v4
        with:
          name: bandit-reports
          path: |
            bandit_report.txt
            bandit_results.sarif
            bandit_summary.md
          retention-days: 30
        if: always()
      
      - name: 📋 Generate summary
        if: always()
        run: |
          echo "## 🔒 Security Scan Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          # Compter les issues
          if [ -f bandit_results.sarif ]; then
            HIGH=$(grep -c '"level": "error"' bandit_results.sarif || echo 0)
            MEDIUM=$(grep -c '"level": "warning"' bandit_results.sarif || echo 0)
            LOW=$(grep -c '"level": "note"' bandit_results.sarif || echo 0)
            
            echo "| Severity | Count |" >> $GITHUB_STEP_SUMMARY
            echo "|----------|-------|" >> $GITHUB_STEP_SUMMARY
            echo "| 🔴 High | $HIGH |" >> $GITHUB_STEP_SUMMARY
            echo "| 🟡 Medium | $MEDIUM |" >> $GITHUB_STEP_SUMMARY
            echo "| 🔵 Low | $LOW |" >> $GITHUB_STEP_SUMMARY
          else
            echo "✅ No security issues found!" >> $GITHUB_STEP_SUMMARY
          fi
          
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Detailed Report" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Download the artifacts to see the full bandit report." >> $GITHUB_STEP_SUMMARY
